import styled from "styled-components";
import { ContainerStyled, WrapperStyled } from "../../components/mockups/MockupsStyles";

export const SearchContainer = styled(ContainerStyled)``

export const SearchWrapper = styled(WrapperStyled)`
    flex-direction: column;
    gap: 40px;
    padding-top: 40px;
    padding-bottom: 40px;
`