import styled from "styled-components";

export const FiltroItem = styled.div`
    flex-direction: column;
    display: flex;
    gap: 10px;
    padding: 5px 16px;

    span {
        color: var(--gray-400);
    }
`

export const FiltroCheckboxItems = styled.div`
    flex-direction: column;
    display: flex;
`